<%= link_to gravatar_for(current_user, size:50),  users_path(current_user.id) %>
<h1><%= current_user.username %></h1>
<span><%= link_to 'view my profile', users_path(current_user.id) %></span>
<spsn><%= pluralize(current_user.microposts.count, 'microost') %></spsn>